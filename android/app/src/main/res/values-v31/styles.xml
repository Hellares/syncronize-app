<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ ignora launch_background y usa la Splash Screen API:
         sin este override mostraba fondo blanco del sistema. Mismo azul +
         logo que el SplashScreen de Flutter. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_image</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>

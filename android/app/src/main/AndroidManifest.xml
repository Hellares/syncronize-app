<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permisos necesarios para videos y audio -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Push Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Cámara (escaneo de códigos de barras / QR) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- Bluetooth Thermal Printer -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- GPS del repartidor (delivery local): tracking con la pantalla
         apagada exige foreground service con notificación visible -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">
        <!-- launchMode=singleTask, NO singleTop: al compartir desde WhatsApp el
             intent sale desde la tarea de WhatsApp. Con singleTop Android solo
             reutiliza la Activity si ya está encima de ESA misma tarea, así que
             con la app en segundo plano nacía una SEGUNDA instancia y quedaban
             dos apps abiertas (dos motores Flutter, dos estados). singleTask
             garantiza una sola: trae al frente la tarea existente y entrega el
             intent por onNewIntent.
             Iba de la mano con taskAffinity="": sin afinidad la Activity no
             tenía tarea propia y se colgaba de la de WhatsApp. Se quitó para
             que el sistema pueda encontrar la tarea de la app. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Flutter, por defecto, agarra la URI del intent (el `geo:` de
                 "Abrir con") y se la pasa al router de Dart como si fuera una
                 ruta de la app. GoRouter no matchea "geo:-8.18,-78.99" y pinta
                 "Page not found". Acá las ubicaciones entran por el
                 MethodChannel de MainActivity, no por el router, así que se
                 apaga. La app no usa app links ni rutas por URL. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Recibir ubicación compartida desde WhatsApp (Compartir → Syncronize).
                 WhatsApp comparte el link de Maps como texto plano. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <!-- Complemento: esquema geo: no requiere verificación de Digital
                 Asset Links en Android 12+, a diferencia de https://maps.google.com -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="geo"/>
            </intent-filter>
        </activity>
        <!-- FCM: Canal de notificaciones por defecto -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="syncronize_default" />
        <!-- FCM: Ícono por defecto para notificaciones -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <!-- FCM: Color del ícono de notificación -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />

        <!-- Google Maps SDK (picker de delivery). Key restringida por
             package (com.syncronize.app y .dev) + SHA-1 — inservible fuera
             de esta app. El SDK móvil no factura por mostrar el mapa. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDoxNsDqwpsQgU1MRIh4doqje8X-wqG9tw" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Abrir la app Yape desde el flujo de pago del marketplace
             (package visibility, Android 11+). -->
        <package android:name="com.bcp.innovacxion.yapeapp"/>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="yape"/>
        </intent>
        <!-- WhatsApp y WhatsApp Business por separado: un celular con dos
             chips suele tener las dos, y hay que poder preguntar cuál abrir.
             Sin declararlas acá, Android 11+ las esconde y `canResolveActivity`
             devuelve false aunque estén instaladas. -->
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
    </queries>
</manifest>
